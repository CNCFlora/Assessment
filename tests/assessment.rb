require_relative "../model/assessment"
require 'rspec'

describe "Assessment" do
    it "CRUD with metadata" do
    end
    it "CRUD with profile and taxon" do
    end
    it "Should add logged user to metadata with creator, created, modifier and modified" do
    end
    it "Should add user as assessor and fill in date of assessment" do
    end
end
